#include<stdio.h>
/* Többsor
*/
#include<stdlib.h>
#include<math.h>

int main()
{
    /* 1. feladat - Kerekítés
    double number;
    scanf("%lf", &number);

    double roundup = ceil(number);
    double rounddown = floor(number);

    printf("My number rounded up = %.2lf\n", roundup);
    printf("My number rounded down = %.2lf", rounddown); */

    /* 2. feladat - Abszolútértékét
    int a = -5;
    printf("Abolute of %d is = %d", a, abs(a)); */
    
    // 3. feladat - Négyzetgyök
    int a;
    scanf("%d", &a);
    printf("Square of %d = %.2lf", a, sqrt(a));

    return 0;
}